# Import the math module to use "pi"
import math

def main():
    """This functions calculates the volume of a cone based off user input"""
    # TODO: Identify the syntax errors and fix them, while adding a comment on what was wrong. Once all errors
    # TODO: are fixed, this program should run as expected. There are 3 syntax errors in total.

    # Prompts the user for input and converts it into an integer (because the input() functions returns a string by default)
    height = int(input("What is height of the cone?: ))
    radius = int(input("What is the radius of the cone?: ")


    # * means multiplication, ** means exponents. 
    coneVolume == (1/3)*math.pi*(radius**2)*height, 2
    

    print("The volume of your cone is approximately: ", coneVolume)



if __name__ == "__main__":
    main()